# Supabase (local dev)
# After running `pnpm supabase:start`, copy the **Publishable** anon key from the output below.
# Run `pnpm supabase:status` to see the keys if you've already started.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=your-anon-key-here